name: Test Go

jobs:
  Test:
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: '1.14'
      - run: go get -u golang.org/x/lint/golint
      - run: go test --cover
